# KOPONTREN AL ITTIHAD — PROGRES / MEMORY / TODO
_Update: 2026-09-18 (Cline session: OPTIMASI PERFORMA — akar masalah
 server-response 15.42s = ~150 round-trip Turso sekuensial di SETIAP
 cold start (db() jalankan seluruh rantai migrasi tiap function dingin).
 Fix: gate `schema_version` (1 SELECT saja di cold start, diuji:
 FULL-INIT 337ms vs FAST-PATH 3ms). + index hot-list
 (sales(created_at,status), sale_items(sale_id,product_id),
 products(active,category)); cache headers (immutable static, ikon
 1 bln, sw.js/manifest no-cache); sw.js v8 + precache shell; favicon.ico
 baru; vercel.json region sin. Turso region (Tokyo/Singapura) = panduan
 manual di dashboard. next.config (compress/poweredBy/sourceMaps)
 sudah ada sebelumnya.)_

## GOAL
Aplikasi kasir & pembukuan Kopontren Al Ittihad (Next.js + Turso/libSQL, PWA).
Pola kerja: kembangkan di branch `master`; `main` = cermin utk Vercel
(Vercel build dari `main`; commit terakhir main `d1f9e56` = sync docs,
sebelumnya `5b072c6` gitignore + `c2b865a` ikon). Master = `e5b01c6`
(state) <- `5f6e6dc` (gitignore cleanup) <- `eda03c7` (ikon lucide).

## STATE
### DONE
- [x] OPTIMASI PERFORMA (18 Sep 2026) — laporan user: server response
  15.42s, finish 19.56s, 51 requests:
  - AKAR MASALAH: `src/db.ts` — fungsi `db()` menjalankan SELURUH rantai
    migrasi (exec(SCHEMA) ±17 stmt, migrate() ±35 execColumn/exec, 8
    UPDATE normalisasi timestamp, seed) di SETIAP cold start function
    Vercel. Tiap statement = 1 round-trip HTTP ke Turso -> ~150 RTT
    sekuensial (±15 detik). Sekarang: gate `schema_version` — cold start
    hanya 1 `SELECT version FROM schema_version`; full init hanya untuk
    DB baru / upgrade skema (naikkan `SCHEMA_VERSION` tiap migrasi baru,
    fullInit jalan sekali lalu stamp ulang).
  - DIBUAT test gate (script temp, dihapus): fresh DB FULL-INIT 337ms,
    cold start ke-2 FAST-PATH 3ms (±100x lebih cepat).
  - INDEX baru (SCHEMA utk DB baru + migrate() utk DB lama):
    `idx_sales_created_status(sales(created_at,status))`,
    `idx_sale_items_sale(sale_items(sale_id,product_id))`,
    `idx_products_active(products(active,category))`.
  - `next.config.mjs`: `headers()` — /_next/static/* immutable 1 tahun;
    /icon-* /logo-* /favicon.ico 1 bulan; /sw.js & /manifest.json
    no-cache (biar update PWA sampai ke user). compress/poweredByHeader/
    productionBrowserSourceMaps sudah ada sejak sebelumnya.
  - `public/sw.js` v7->v8: precache [/manifest.json, /icon-192.png,
    /favicon.ico] saat install; /favicon.ico ikut cache-first. Size
    ±2.4KB raw (±0.9KB gzip) — tetap ringan karena statis+no-cache.
  - `public/favicon.ico` BARU (10.8KB, wrapper ICO dari icon-192.png via
    script node) — sebelumnya request /favicon.ico = 404/303ms.
  - `vercel.json` BARU: `{"regions": ["sin"]}` — function Vercel di
    Singapura (paling dekat user Indonesia); asset tetap CDN global.
  - Audit N+1: /api/sales GET sudah batched (3 query IN clause,
    bukan loop per transaksi); /api/products = 2 query; backfill
    qr_code di /api/members hanya jalan sekali utk member legacy
    (dibiarkan).
  - TIDAK pakai next/font: app memakai font sistem (0 request font web)
    — menambah web font justru bikin HP entry-level makin berat.
    Code-splitting per route Next.js App Router sudah default.
- [x] Sidebar navigasi (18 Sep 2026) — menggantikan nav pill horizontal
      (yang harus di-scroll di HP):
      - `src/components/sidebar.tsx` (baru, 'use client'): `Sidebar`
        (drawer slide-in dari kiri + overlay gelap `bg-navy-900/60`,
        translate-x, panel `transition-transform duration-300 ease-in-out`,
        overlay `transition-opacity duration-300`; lebar 280px HP /
        260px desktop (`md:w-[260px]`); tutup via klik overlay / Escape /
        klik menu / tombol ✕; scroll latar dikunci saat terbuka; item
        aktif = bg-accent-500 text-white) + `HamburgerNav` (tombol ☰,
        state `open` di sini).
      - Posisi tombol ☰: KIRI logo — urutan header
        `[☰] [Logo] [Nama Toko] ... [User] [Tema] [Keluar]`.
      - FIX sidebar terpotong (hanya 1 menu kelihatan): header punya
        `backdrop-blur` yang bikin *containing block* untuk descendant
        `fixed`, jadi `inset-y-0` panel relatif ke header (~60px).
        `Sidebar` sekarang di-render via `createPortal(document.body)`
        (aman SSR); struktur tetap `flex flex-col` + header panel
        `shrink-0` + `<nav>` `flex-1 overflow-y-auto` (isi penuh tinggi
        layar, scroll hanya jika menu melebihi tinggi layar).
      - Grup menu per role: admin = Utama (Ringkasan, Kasir, Laporan &
        Rekap) + Admin (Produk, Belanja, Konsinyasi, Piutang, Retur,
        Kas, Shift & Kasir) + Loyalty (Keuntungan Member, Member) +
        Sistem (Audit, Laporan Pengurus, Pengguna, Data & Backup);
        pengurus = Ringkasan, Laporan & Rekap, Laporan Pengurus;
        kasir = Ringkasan, Kasir, Laporan & Rekap (spesifikasi user;
        halaman /piutang & /retur tetap bisa dibuka kasir via URL).
      - `shell.tsx`: baris `<nav>` pill dihapus; header jadi 1 baris
        (☰ + logo + judul + chip user + tema + keluar); `min-w-0` +
        `truncate` pd judul agar aman di layar sempit.
      - `src/components/navpills.tsx` DIHAPUS (tak terpakai) —
        termasuk dari `main` (sisa strategi file-copy; commit `8bad685`).
      - `npx tsc --noEmit` EXIT=0. Test visual HP/desktop = manual
        (environment tanpa browser).
      - Git: commit `2fdad8f` ke `master`; sync `main` `8bad685`
        (file-copy + `git rm navpills.tsx`); push keduanya.
- [x] Penggantian SEMUA emoji dengan ikon SVG (18 Sep 2026):
      Pakai library `lucide-react` (^1.47.0, `npm install`) alih-alih
      SVG custom — lebih konsisten & ringan (tree-shake). Scan regex
      seluruh `src/` = 0 emoji tersisa.
      - `themetoggle.tsx`: ☾/☀ -> <Moon/>/<Sun/>.
      - `ui.tsx` (Modal): tombol tutup ✕ -> <X/>.
      - `sidebar.tsx`: trigger ☰ -> <Menu/>, tombol tutup ✕ -> <X/>.
      - `data-client.tsx`: 📦/📊/📥/⚠️ -> <Package/>/<BarChart/>/
        <Upload/>/<AlertTriangle/> (kartu, ikon h-5 w-5 di wadah h-9 w-9).
      - `member-client.tsx`: ★ badge poin -> <Star fill="currentColor"/>.
      - `laporan-client.tsx`: 📊 Unduh CSV -> <BarChart/>, 📱 Rekap WA
        -> <MessageCircle/>, 📄 state kosong -> <FileText/>.
      - `pos-client.tsx`: 🔍 -> <Search/>, ✕ (search & hapus item) ->
        <X/>, 📦/🛒 state kosong -> <Package/>/<ShoppingCart/>, 📱 input
        phone -> <Smartphone/>, ⛶ Scan QR -> <QrCode/>, ★ member terpilih
        -> <Star/>, tombol bayar 💵/📱/🏦 -> <Banknote/>/<QrCode/>/
        <Landmark/>, tombol struk 📋/💬/🖨️ -> <Clipboard/>/
        <MessageCircle/>/<Printer/>, ✓ shift ditutup -> <CheckCircle/>.
      - Git: `master` `eda03c7` + cleanup `5f6e6dc`; sync `main`
        `c2b865a` + `5b072c6`; push keduanya. `npx tsc --noEmit` exit 0.
      - Ikon gaya outline (stroke), warna mengikuti `currentColor`
        (otomatis adjust utk dark mode); bintang pakai fill.

- [x] Fitur Piutang (debts) & Retur (returns) — batch sesi 18 Sep:
      - API: `src/app/api/debts/route.ts` (GET daftar+summary / POST catat;
        kasir+admin bisa tulis, pengurus read-only, pengurus tak boleh).
      - API: `src/app/api/debts/[id]/route.ts` (PATCH terima-bayar `pay`
        max=sisa, sisa=0 -> status 'settled' otomatis; update note/due_date/
        status; DELETE hanya admin).
      - API: `src/app/api/returns/route.ts` (GET riwayat / POST catat retur:
        validasi transaksi & produk milik transaksi, kasir hanya transaksi
        sendiri; dalam `tx`: insert returns + restock products + (opsional)
        jurnal cash_entries type='expense' bila refund tunai).
      - Halaman: `/piutang` & `/retur` (role kasir+admin; pengurus ->
        redirect '/'), client `piutang-client.tsx` (filter open/settled/all,
        badge Tunggak, modal terima bayar, hapus utk admin) &
        `retur-client.tsx` (pilih transaksi 30 hari -> pilih produk di
        transaksi -> qty -> refund checkbox -> riwayat).
      - Nav pills (`shell.tsx`): 'Piutang' & 'Retur' utk admin + kasir.
      - Dashboard (`page.tsx`): kartu "Piutang belum lunas" (admin & kasir).
      - `/api/sales` GET: kolom `product_id` ditambahkan ke query items
        (aditif; dibutuhkan form retur utk memetakan produk).
      - `npx tsc --noEmit` EXIT=0 (bersih).
      - Git: commit `6a7e8c9` ke `master`, sync ke `main` (file-copy,
        tanpa common ancestor), push origin main+master; Vercel
        auto-deploy main. Selesai 18 Sep 2026.
- [x] (sebelumnya) Loyalty: redeem poin, ledger point_history, QR reset,
      field struk WA; QR badge member; cari member via HP di POS;
      fix Next.js 15 params (inline Promise<{id:string}>) di members/[id].
- [x] (sebelumnya) Migrasi lokal node:sqlite -> @libsql/client (Turso);
      optimasi N+1 /api/sales; index db; cache sw v7; resize logo &
      ikon PNG; build lokal green.

### IN PROGRESS
- (tidak ada)

### BLOCKED / CATATAN
- **Relasi branch:** `main` dan `master` TIDAK punya common-ancestor
  (main dibuat dari "Add files via upload" Vercel). Sync dilakukan via
  `git checkout main; git checkout master -- .; commit` (bukan merge).
- **Git path:** `git.exe` ada di `C:\Program Files\Git\cmd\git.exe` tapi
  BELUM ada di PATH sesi proses baru (winget install) -> pakai path penuh
  di skrip; VS Code lama mungkin sudah lihat PATH lama.
- **Backup Turso live:** kredensial Turso hanya di env Vercel; lokal
  `.env` = `DATABASE_URL=file:./data/kopontren.db`. Perubahan skema
  aditif (IF NOT EXISTS / ALTER guard) -> aman utk data; disarankan
  backup in-app (Admin -> Data & Backup) sebelum fitur besar.
- **Tes browser/Lighthouse:** environment tanpa browser -> tes manual di
  HP (Slow 3G + instal PWA).

## NEXT / TODO
- [x] Commit batch sidebar navigasi: `master` `b409dfb`, sync `main`
      `99dc587` (+ commit kecil pd main utk hapus `navpills.tsx` sisa —
      strategi file-copy tidak menghapus file), push keduanya.
- [x] (18 Sep, lanjutan) hamburger di KIRI logo + sidebar 280/260px +
      animasi 300ms: `master` `2fdad8f`, sync `main` `8bad685`
      (commit ini jugalah yg benar-benar `git rm navpills.tsx` dari
      main); `git ls-tree main/master -- src/components/navpills.tsx`
      = kosong di keduanya. Push keduanya.
- [x] Keputusan: TIDAK pakai full-sync `git read-tree -u --reset
      master` (cara 2) pd main — `main` punya 21 file ekstra yg
      disengaja (sw.js, manifest.json, ikon PWA, dll; beda arah diff
      = semua insert). navpills sudah bersih via `git rm`, jadi
      cukup re-deploy Vercel (triggered oleh push main berikutnya).
- [ ] Check region Turso di dashboard (Database -> Region) / CLI
      `turso db show`; jika US -> migrasi Singapore/Tokyo
      (`turso db create --region aws-ap-southeast-1` + clone + ganti
      env Vercel DATABASE_URL/TOKEN). Lalu jalankan Lighthouse
      (Chrome DevTools, profil Mobile/Slow 4G) — target finish time
      < 5 detik & TTFB < 1 detik.
- [ ] Test visual: HP (Chrome DevTools device mode — pastikan TIDAK ada
      scroll horizontal; sidebar membuka dari kiri + overlay, tombol ☰
      di kiri logo; SEMUA ikon lucide tampil — search, tombol bayar,
      struk, QR, star member, badge poin) & desktop (open/close
      sidebar, item aktif, Escape, lebar 280/260px).
- [ ] Pantau deployment Vercel main (`d1f9e56` = terakhir yg di-push;
      commit aplikasinya `c2b865a` ikon + `5b072c6` gitignore) —
      build green; jika gagal walau branch sudah bersih -> Redeploy
      manual di dashboard Vercel.
- [ ] Uji manual fitur baru di produksi: catat piutang, terima bayar
      (sisa=0 -> lunas), catat retur (restock + jurnal kas keluar bila
      refund), badge Tunggak.
- [x] (Opsional, selesai 18 Sep) Kolom rekap piutang/retur pd `/api/reports`
      + Laporan Pengurus: response baru `debts {open_total, open_count,
      new_count, new_total, settled_total, paid_total}` &
      `returns {count, total, refund_total}` (refund di-ambil dari jurnal
      cash_entries berlabel 'Retur #%'). `laporan-admin-client.tsx` nambah
      section "Rekap piutang & retur" (4 kartu). Catatan: TIDAK ada tabel
      debt_payments — pembayaran piutang update `debts.paid` in-place.
- [ ] (Opsional) Multi-store: tabel `stores` sudah ada di skema +
      `sales.store_id` (kosong) — butuh pemilihan store di POS +
      filter laporan per store.
- [ ] (Opsional) Fitur bundel/kategori/produk-harga-grosir (tabel
      `bundles`, `categories`, `product_prices` sudah ada di skema,
      API & UI belum).

## HIGHLIGHTS / MEMORY
- Pola izin api: `currentUser()` -> 401; pengurus -> 403 utk tulis;
  kasir dibatasi ke data miliknya (sale.kasir_id === user.id).
- Refund retur TIDAK memotong cash_entries manual — dicatat otomatis
  sebagai jurnal expense berlabel 'Retur #<saleId> · <nama produk>'
  agar pembukuan kas tetap akur (termasuk di /api/kas & dashboard).
- `todayStr()` di piutang-client memakai local date (WIB di Indonesia)
  utk deteksi tunggak (due_date < hari ini & status open).
- `Modal`, `Badge`, `Toast`, `useToast`, helper `api()` ada di
  `src/components/ui.tsx`; format `rp/fmtDateTime` di `src/lib/format.ts`.
- Helper bertanda underscore gitignore: pola `_*` menyeluruh sejak
  commit `5f6e6dc` (sebelumnya hanya `_*.{bat,ps1,txt}` + `*.log` —
  celah itu bikin `_scanemoji2.mjs` lolos masuk commit, lalu dibuang).
- Ikon UI seragam via `lucide-react` (lengkap di commit `eda03c7`);
  jangan campur emoji lagi — tambah ikon dari lucide (import dari
  `lucide-react`, stroke `currentColor`, ukuran `h-N w-N`).
- PERFORMA: jangan tambah statement migrasi baru di `db()` tanpa
  menaikkan `SCHEMA_VERSION` (fullInit jalan sekali, lalu cold start
  cukup 1 SELECT `schema_version`). Turso: region check di dashboard
  Turso (Database -> Region) / CLI `turso db show`; migrasi region =
  `turso db create <nama> --region aws-ap-southeast-1` lalu
  `turso db clone <lama> <nama>` + update env DATABASE_URL/TOKEN.

## FILES
- `src/db.ts` (gate `schema_version` + fullInit + 3 index baru, commit
  ini), `next.config.mjs` (headers cache), `public/sw.js` (v8 precache),
  `public/favicon.ico` (baru), `vercel.json` (region sin)
- `src/app/api/debts/route.ts`, `src/app/api/debts/[id]/route.ts`,
  `src/app/api/returns/route.ts`
- `src/app/api/sales/route.ts` (items +product_id)
- `src/app/piutang/page.tsx`, `src/app/retur/page.tsx`
- `src/components/piutang-client.tsx`, `src/components/retur-client.tsx`
- `src/components/sidebar.tsx` (drawer + HamburgerNav; menggantikan
  `navpills.tsx` yang dihapus), `src/components/shell.tsx`
  (header 1 baris + ☰), `src/app/page.tsx` (kartu piutang)
- Ikon lucide-react (commit `eda03c7`): `src/components/pos-client.tsx`,
  `src/components/themetoggle.tsx`, `src/components/ui.tsx`,
  `src/components/laporan-client.tsx`, `src/components/admin/
  data-client.tsx`, `src/components/admin/member-client.tsx`
- `src/db.ts` (tabel debts/returns sudah ada sejak feature batch 2),
  `state.txt` (file ini)
